# ใช้สำหรับ `prisma generate`/`prisma validate` เท่านั้น (ตอน build) — ไม่มีผลตอน runtime จริง
# runtime จริงรับ DATABASE_URL จาก electron/main.cjs (ชี้ไปที่ userData ของผู้ใช้)
DATABASE_URL="file:./data/bms_finance.db"
